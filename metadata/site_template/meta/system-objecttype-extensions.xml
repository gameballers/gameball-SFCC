<?xml version="1.0" encoding="UTF-8"?>
<metadata xmlns="http://www.demandware.com/xml/impex/metadata/2006-10-31">
    <type-extension type-id="SitePreferences">
        <custom-attribute-definitions>
            <attribute-definition attribute-id="gameballEnabled">
                <display-name xml:lang="x-default">Enable Gameball</display-name>
                <description xml:lang="x-default">Turn the Gameball Integration on or off. Also requires the "gameball.http.api.cred" Service Credential (Administration &gt; Operations &gt; Services &gt; Service Credentials) to be filled in with a valid API Key (User ID) and Secret Key (Password).</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballExcludeGiftCertificateTender">
                <display-name xml:lang="x-default">Exclude Gift Certificate Tender from Gameball totalPaid</display-name>
                <description xml:lang="x-default">When enabled (the default), the portion of an order paid for with a Gift Certificate is subtracted from the totalPaid amount reported to Gameball, so points are not awarded on gift-certificate-funded spend.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <!-- item 02 -->
            <attribute-definition attribute-id="gameballInfoLogEnabled">
                <display-name xml:lang="x-default">Gameball Info Logging</display-name>
                <description xml:lang="x-default">Writes one INFO line to the custom Gameball log for every customer upsert the storefront sends or deliberately skips. This is the only way an operator can confirm that exactly one Gameball customer upsert is sent per registration and per profile save - count the "upsert sent" lines sharing one sfccRequestId. Turning it off silences the success lines, the skip lines AND the transient-failure warnings (rate limit, service timeout, Gameball-side 5xx), which means a Gameball outage stops appearing in the log at all; only permanent errors are written regardless of this setting. Leave it on unless log volume forces otherwise.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballDebugLogEnabled">
                <display-name xml:lang="x-default">Gameball Debug Logging</display-name>
                <description xml:lang="x-default">Writes DEBUG lines for the high-volume, uninteresting Gameball skip paths - chiefly "integration disabled", which would otherwise fire on every profile save on a site where Gameball is switched off. Off by default; switch it on only while diagnosing why an expected sync did not happen.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <!-- item 03 -->
            <attribute-definition attribute-id="gameballSyncOcapiShopCustomers">
                <display-name xml:lang="x-default">Sync OCAPI/SCAPI Shop API Customers</display-name>
                <description xml:lang="x-default">When enabled, a customer registered or edited through the OCAPI/SCAPI Shop API (a headless storefront) is synced to Gameball immediately, exactly as a storefront registration is. Requires Administration &gt; Global Preferences &gt; Feature Switches &gt; API hook execution to be turned on, which is off by default and cannot be detected from script - if that switch is off, this preference has no effect and the Gameball Customer Delta job is the only coverage for these customers.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballSyncDataApiCustomers">
                <display-name xml:lang="x-default">Sync OCAPI/SCAPI Data API Customers</display-name>
                <description xml:lang="x-default">When enabled, a customer created, replaced or patched through the OCAPI/SCAPI Data API (typically a CRM, OMS or middleware) is synced to Gameball inside the same request. Leave disabled for high-volume bulk upserts: the Data API hook runs inside the platform's own database transaction, so each record would hold a transaction open for the duration of a synchronous Gameball call. Coverage is not lost by leaving this off - the Gameball Customer Delta job picks those customers up within its schedule interval without holding the Data API request open.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballCustomerRequireEmail">
                <display-name xml:lang="x-default">Require an Email Before Syncing a Customer</display-name>
                <description xml:lang="x-default">When enabled, a customer profile with no email address is skipped by every Gameball customer sync path instead of being sent, and is recorded as gbSyncState SKIPPED. Turn this on if your Gameball workspace uses email-based channel merging: a profile with no email cannot merge and becomes a permanently orphaned Gameball record. This is a data-quality gate, not a consent gate. Default off, which preserves the previous behaviour exactly.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballCustomerDeltaStrategy">
                <display-name xml:lang="x-default">Customer Delta Job Strategy</display-name>
                <description xml:lang="x-default">How the Gameball Customer Delta job finds changed profiles. LAST_MODIFIED (default) queries profiles by their last-modified date and is the cheap path. PAGED_CUSTOMER_NO pages the whole customer base in customer-number order and filters by last-modified date in code; use it only if LAST_MODIFIED fails on your instance because lastModified is not a queryable Profile attribute, which the job reports by ending with an error naming this preference. The job never switches strategy on its own.</description>
                <type>enum-of-string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <value-definitions>
                    <value-definition default="true">
                        <value>LAST_MODIFIED</value>
                    </value-definition>
                    <value-definition>
                        <value>PAGED_CUSTOMER_NO</value>
                    </value-definition>
                </value-definitions>
            </attribute-definition>
            <attribute-definition attribute-id="gameballCustomerDeltaLookbackHours">
                <display-name xml:lang="x-default">Customer Delta Lookback (Hours)</display-name>
                <description xml:lang="x-default">How far back the Gameball Customer Delta job looks for modified profiles on each run. Must comfortably exceed the job's schedule interval so a skipped or failed run is recovered by the next one, and must exceed the customer search index lag (budget 1 to 1.5 hours). In PAGED_CUSTOMER_NO strategy it must also exceed the time one full pass over the customer base takes. Profiles that fall out of this window stop being retried, which is what bounds a permanently failing profile.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>48</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballCustomerDeltaMaxCallsPerRun">
                <display-name xml:lang="x-default">Customer Delta Max Gameball Calls Per Run</display-name>
                <description xml:lang="x-default">Upper bound on the number of Gameball customer upserts the delta job issues in a single run. Gameball caps customer POSTs at 30 per second and 360 per rolling 30 seconds account-wide, shared with every other site and every storefront call, so raising this materially requires a matching schedule change. The job ends cleanly with OK when the budget is spent; unsent profiles are picked up on the next run. A bulk customer import is a backfill in disguise - at the default of 500 per hourly run it drains at 12,000 profiles a day.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>500</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballCustomerDeltaMaxProfilesPerRun">
                <display-name xml:lang="x-default">Customer Delta Max Profiles Read Per Run</display-name>
                <description xml:lang="x-default">Upper bound on the number of profiles the delta job reads in a single run. Only used by the PAGED_CUSTOMER_NO strategy, where it is the termination guarantee and sets how fast one full pass over the customer base completes; the LAST_MODIFIED strategy is bounded by its own query instead. Reading is cheap - this is not a Gameball call budget, and lowering it does not reduce API traffic.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>20000</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballAllowNonProductionSync">
                <display-name xml:lang="x-default">Allow Gameball Jobs on Non-Production Instances</display-name>
                <description xml:lang="x-default">Every Gameball job refuses to run on a Sandbox, Development or Staging instance unless this is explicitly enabled, and reports that refusal as a successful run. A sandbox data refresh copies production customer and order data; without this guard a refreshed sandbox would start pushing production records into the configured Gameball workspace unattended within one job interval. Enable it only on an instance pointed at a Gameball test workspace.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballMaxRequestsPerSecond">
                <display-name xml:lang="x-default">Gameball Max Requests Per Second</display-name>
                <description xml:lang="x-default">Soft ceiling every Gameball job paces itself against. Gameball's published limits are 30 requests per second and 360 per rolling 30 seconds, and they are ACCOUNT-scoped: they are shared by every job, every site and every storefront call against one Gameball workspace. A multi-site merchant therefore DIVIDES this value across the sites sharing a workspace - it is never multiplied. SFCC has no sleep, so the only throttle available to a job is to stop issuing calls, which is what happens when this ceiling is reached; the run ends cleanly and the next one resumes.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>10</default-value>
            </attribute-definition>
            <!-- item 04 -->
            <attribute-definition attribute-id="gameballErasureEnabled">
                <display-name xml:lang="x-default">Enable Gameball Customer Erasure</display-name>
                <description xml:lang="x-default">Master switch for the Gameball Customer Erasure job. When OFF, right-to-be-forgotten requests are still captured and stored - nothing is lost - but no customer is deleted from Gameball. Turning it ON later drains whatever backlog is still inside the 14-day retention window. Gameball's delete is a HARD delete with no undo and no anonymize alternative, so read docs/gameball-gdpr.md before enabling it. Note that capture is deliberately NOT gated by this preference: the Gameball customer id is destroyed by the SFCC deletion itself, so it has to be recorded whether or not the drain is switched on.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballErasureMaxAttempts">
                <display-name xml:lang="x-default">Gameball Erasure Max Attempts</display-name>
                <description xml:lang="x-default">How many times the Gameball Customer Erasure job calls Gameball for one customer before marking the request FAILED. The job runs every 30 minutes, so the default of 5 is roughly a 2.5-hour recovery window for a transient Gameball outage. Attempts refused by SFCC itself - an open circuit breaker or the service rate limiter - do not count, because a valve the platform closed must not burn a legal mandate's budget; a request stuck that way is instead marked FAILED once it has been PENDING for 7 days, so it cannot expire unnoticed. A FAILED request turns the job red on every subsequent run until it is resolved, and is removed by platform retention 14 days after it was first captured; it must be resolved inside that window or the mandate is lost and has to be re-issued from the Gameball dashboard. See docs/gameball-gdpr.md for the two ways to resolve one.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>5</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballErasureSuccessRetentionDays">
                <display-name xml:lang="x-default">Gameball Erasure Success Retention (Days)</display-name>
                <description xml:lang="x-default">How long a completed erasure request is kept before the job deletes it. The row holds the shopper's Gameball customer id, which is exactly the pseudonymous identifier the erasure request existed to remove, so this is deliberately short. The Custom Object's own 14-day retention is the backstop, not the policy.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>7</default-value>
            </attribute-definition>
            <!-- item 05 -->
            <attribute-definition attribute-id="gameballTrackGuestOrders">
                <display-name xml:lang="x-default">Track Guest Orders in Gameball</display-name>
                <description xml:lang="x-default">Master switch for guest order tracking. OFF by default: while it is off, any order placed without a registered customer profile (guest checkout, POS, imported and OMS-created orders) is recorded as gbTrackState=SKIPPED and earns no points, which is the behaviour of every release before this one. Turning it on makes Gameball auto-create a customer profile for each guest identity produced by gameballGuestOrderMode. Read gameballGuestOrderMode's description before enabling: guest identities cannot be re-keyed or merged afterwards by this cartridge.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballGuestOrderMode">
                <display-name xml:lang="x-default">Guest Order Identity Mode</display-name>
                <description xml:lang="x-default">How a Gameball customerId is derived for an order that has no registered customer profile. Only consulted when gameballTrackGuestOrders is on. PER_ORDER (default) mints "gb_guest_" + the SFCC order number, so every guest order becomes its own Gameball profile - deterministic, collision-free, and safe to retry, but a repeat guest never accumulates a balance. PER_EMAIL mints "gb_guest_" + the SHA-256 hex of the lower-cased, trimmed customer email joined to the site id, so the same email on the same site always resolves to one profile that accumulates points; when the order carries no usable email (POS, imported and OMS-created orders) it falls back to PER_ORDER and never derives an id from an empty value. SKIP tracks nothing, leaving guest orders SKIPPED. CHANGING THIS VALUE DOES NOT MIGRATE ALREADY-TRACKED ORDERS: their stored gbCustomerId is never rewritten, so points already earned under the old scheme stay where they are.</description>
                <type>enum-of-string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <value-definitions>
                    <value-definition default="true">
                        <display xml:lang="x-default">PER_ORDER</display>
                        <value>PER_ORDER</value>
                    </value-definition>
                    <value-definition>
                        <display xml:lang="x-default">PER_EMAIL</display>
                        <value>PER_EMAIL</value>
                    </value-definition>
                    <value-definition>
                        <display xml:lang="x-default">SKIP</display>
                        <value>SKIP</value>
                    </value-definition>
                </value-definitions>
            </attribute-definition>
            <attribute-definition attribute-id="gameballLinkGuestOrdersByLogin">
                <display-name xml:lang="x-default">Link Guest Orders to a Matching Customer Login</display-name>
                <description xml:lang="x-default">When on (the default), an order placed without logging in is first checked against CustomerMgr.getCustomerByLogin(order email); if a customer is found the order is attributed to that registered profile with guest=false, so a shopper who forgot to sign in still earns on their real balance. CAVEAT: getCustomerByLogin matches on the LOGIN, not on the email address. On a storefront where login and email are the same string (the SFRA default) this works; on a storefront using a separate username it silently never matches and every such order falls through to gameballGuestOrderMode. Turn this off if guest checkout must never touch a registered balance. Only consulted when gameballTrackGuestOrders is on.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <!-- item 06 -->
            <attribute-definition attribute-id="gameballRetryEnabled">
                <display-name xml:lang="x-default">Enable Gameball Failed-Order Retry</display-name>
                <description xml:lang="x-default">Master switch for the Gameball Order Retry job step. When off the step logs and exits OK without scanning any orders. Independent of gameballEnabled so the retry sweep can be paused without turning off live order tracking.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRetryLookbackDays">
                <display-name xml:lang="x-default">Gameball Retry Lookback (days)</display-name>
                <description xml:lang="x-default">How far back the retry sweep looks, as an absolute floor on the order creation date. This is a lower bound only - never a moving window - so an order stays visible to every run until it reaches a terminal state or ages past the floor. Raise it to recover an older backlog. Clamped in code to 1-90.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>7</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRetryMaxAttempts">
                <display-name xml:lang="x-default">Gameball Retry Max Attempts</display-name>
                <description xml:lang="x-default">How many retry attempts an order gets before it is settled to the terminal RETRY_EXHAUSTED state and never re-sent. Counts the original confirmation-page attempt as attempt 1. Clamped in code to 1-20.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>5</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRetryMaxOrdersPerRun">
                <display-name xml:lang="x-default">Gameball Retry Max Orders Per Run</display-name>
                <description xml:lang="x-default">Hard ceiling on the number of outbound Gameball calls (POST plus verification GET) a single run may issue. Once reached the run stops calling and leaves the remaining orders for the next run. This is the primary throughput brake - SFCC has no sleep primitive. Clamped in code to 1-2000.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>200</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRetryBackoffMinutes">
                <display-name xml:lang="x-default">Gameball Retry Backoff Base (minutes)</display-name>
                <description xml:lang="x-default">Base of the exponential spacing between attempts on one order: attempt N is not retried before base x 2^(N-1) minutes after the previous attempt, capped at 8 hours. Prevents a two-hour Gameball outage from burning an order's whole attempt budget in two job runs. Clamped in code to 0-1440.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>30</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRetryProbeBeforeResend">
                <display-name xml:lang="x-default">Verify Before Re-sending Ambiguous Gameball Orders</display-name>
                <description xml:lang="x-default">When on (recommended), an order whose previous failure was ambiguous - a timeout, a connection fault or an HTTP 500, where the POST may in fact have succeeded - is first checked with a read-only GET of that order's Gameball transactions, and is only re-POSTed if Gameball has no reward transaction for it. Turn this off only after confirming with Gameball that a duplicate orderId is rejected rather than double-awarded.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <!-- item 07 -->
            <attribute-definition attribute-id="gameballEnableRefunds">
                <display-name xml:lang="x-default">Enable Gameball Refunds</display-name>
                <description xml:lang="x-default">Master switch for sending refunds and cancellations to Gameball. Off by default. IMPORTANT - read before enabling: the automatic path catches roughly 10-20% of refund events by count and less by value (full pre-shipment cancellations that flip the order status), and 0% of post-shipment returns, partial refunds, appeasements, shipping-only or tax-only refunds and chargebacks, and approximately 0% at any merchant whose refunds are issued by an external OMS or from the payment provider's own dashboard. Everything beyond that must be pushed in by your own code calling gameballRefundApi.submitRefund(). See docs/refunds-integration-guide.md.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRefundDetectCancellations">
                <display-name xml:lang="x-default">Detect Order Cancellations Automatically</display-name>
                <description xml:lang="x-default">When on (the default), the Gameball Refund Detector job sends a full points reversal for any order that was successfully tracked to Gameball and has since moved to CANCELLED or FAILED status. Turn this off if an external system already reports those cancellations to Gameball via gameballRefundApi.submitRefund(), otherwise the reversal would be attempted twice (Gameball would answer 9004 and the second attempt would be harmless, but the duplicate call is wasted).</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>true</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRefundLookbackDays">
                <display-name xml:lang="x-default">Refund Detector Lookback (Days)</display-name>
                <description xml:lang="x-default">How far back the Gameball Refund Detector job scans, in days, measured from gbTrackedAt. This is a full re-scan on every run, not a moving window - an order tracked in January and cancelled in March is only ever found because the scan still reaches back to January. Lowering this below your longest realistic cancellation lag will silently stop catching those cancellations. Raising it increases job run time proportionally.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>90</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRefundOrphanMaxHours">
                <display-name xml:lang="x-default">Orphan Refund Max Age (Hours)</display-name>
                <description xml:lang="x-default">How long a refund submitted through gameballRefundApi.submitRefund() for an order that Gameball has not yet been told about is kept waiting before it is abandoned. While waiting, the refund is re-checked on every Refund Detector run and sent as soon as the order reaches TRACKED. After this many hours it is marked SKIPPED and never sent - sending it blind would guarantee a Gameball 9005 "reversed transaction not found".</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>24</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRefundMaxAttempts">
                <display-name xml:lang="x-default">Refund Max Delivery Attempts</display-name>
                <description xml:lang="x-default">How many delivery attempts one refund gets before it is marked FAILED and stops being retried. Attempts are made by the hourly Refund Detector job, so six attempts spans roughly six hours at the default schedule. Attempts refused by Salesforce B2C Commerce itself (rate limiter or open circuit breaker) do not count against this budget.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>6</default-value>
            </attribute-definition>
            <!-- item 08 -->
            <attribute-definition attribute-id="gameballEnableRedemption">
                <display-name xml:lang="x-default">Enable Pay with Points</display-name>
                <description xml:lang="x-default">Master switch for "Pay with Points" - letting a shopper hold and redeem Gameball points as a direct discount on cart/checkout. Off by default. Requires a registered, authenticated shopper; guest baskets never see this feature regardless of this setting.</description>
                <type>boolean</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>false</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRedemptionMaxPercentOfBasket">
                <display-name xml:lang="x-default">Redemption Max Percent of Basket</display-name>
                <description xml:lang="x-default">Caps how much of the basket's eligible total (gross price, before the Gameball discount itself) a single points hold may cover, as a percentage. Gameball's hold endpoint has no awareness of the SFCC basket total - this is the sole SFCC-side guard against a hold that would exceed, or come close to exceeding, the whole order.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>100</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRedemptionMinOrderAmount">
                <display-name xml:lang="x-default">Redemption Min Order Amount</display-name>
                <description xml:lang="x-default">The minimum amount, in the site's default currency, that must remain payable after a points redemption is applied. A requested hold that would drop the basket below this floor is rejected before any Gameball call is made. Some payment processors reject a $0.00 authorization; raise this above 0 if yours does.</description>
                <type>double</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>0</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gameballRedemptionAssumedHoldTtlMinutes">
                <display-name xml:lang="x-default">Redemption Assumed Hold TTL (Minutes)</display-name>
                <description xml:lang="x-default">COSMETIC ONLY - never used for release or enforcement. Neither the hold-create nor the hold-status Gameball endpoint returns an expiry timestamp, so this value is added to "now" purely to show the shopper an approximate countdown, and must be kept in sync with your Gameball dashboard's actual hold-timeout setting by hand - there is no way to read that setting via API.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>30</default-value>
            </attribute-definition>
        </custom-attribute-definitions>
        <group-definitions>
            <attribute-group group-id="GameballConfigs">
                <display-name xml:lang="x-default">Gameball Settings</display-name>
                <attribute attribute-id="gameballEnabled"/>
                <attribute attribute-id="gameballExcludeGiftCertificateTender"/>
                <attribute attribute-id="gameballInfoLogEnabled"/>
                <attribute attribute-id="gameballDebugLogEnabled"/>
                <attribute attribute-id="gameballSyncOcapiShopCustomers"/>
                <attribute attribute-id="gameballSyncDataApiCustomers"/>
                <attribute attribute-id="gameballCustomerRequireEmail"/>
                <attribute attribute-id="gameballCustomerDeltaStrategy"/>
                <attribute attribute-id="gameballCustomerDeltaLookbackHours"/>
                <attribute attribute-id="gameballCustomerDeltaMaxCallsPerRun"/>
                <attribute attribute-id="gameballCustomerDeltaMaxProfilesPerRun"/>
                <attribute attribute-id="gameballAllowNonProductionSync"/>
                <attribute attribute-id="gameballMaxRequestsPerSecond"/>
                <attribute attribute-id="gameballErasureEnabled"/>
                <attribute attribute-id="gameballErasureMaxAttempts"/>
                <attribute attribute-id="gameballErasureSuccessRetentionDays"/>
                <attribute attribute-id="gameballTrackGuestOrders"/>
                <attribute attribute-id="gameballGuestOrderMode"/>
                <attribute attribute-id="gameballLinkGuestOrdersByLogin"/>
                <!-- appended by item 06 -->
                <attribute attribute-id="gameballRetryEnabled"/>
                <attribute attribute-id="gameballRetryLookbackDays"/>
                <attribute attribute-id="gameballRetryMaxAttempts"/>
                <attribute attribute-id="gameballRetryMaxOrdersPerRun"/>
                <attribute attribute-id="gameballRetryBackoffMinutes"/>
                <attribute attribute-id="gameballRetryProbeBeforeResend"/>
            </attribute-group>
            <!-- item 07 -->
            <attribute-group group-id="GameballRefundConfigs">
                <display-name xml:lang="x-default">Gameball Refunds</display-name>
                <attribute attribute-id="gameballEnableRefunds"/>
                <attribute attribute-id="gameballRefundDetectCancellations"/>
                <attribute attribute-id="gameballRefundLookbackDays"/>
                <attribute attribute-id="gameballRefundOrphanMaxHours"/>
                <attribute attribute-id="gameballRefundMaxAttempts"/>
            </attribute-group>
            <!-- item 08 -->
            <attribute-group group-id="GameballRedemptionConfigs">
                <display-name xml:lang="x-default">Gameball Pay with Points</display-name>
                <attribute attribute-id="gameballEnableRedemption"/>
                <attribute attribute-id="gameballRedemptionMaxPercentOfBasket"/>
                <attribute attribute-id="gameballRedemptionMinOrderAmount"/>
                <attribute attribute-id="gameballRedemptionAssumedHoldTtlMinutes"/>
            </attribute-group>
        </group-definitions>
    </type-extension>
    <type-extension type-id="Order">
        <custom-attribute-definitions>
            <attribute-definition attribute-id="gbTrackState">
                <display-name xml:lang="x-default">Gameball Track State</display-name>
                <description xml:lang="x-default">Outcome of syncing this order to Gameball. TRACKED - successfully sent, or Gameball confirmed it already had this order. SKIPPED - excluded by a gating rule: integration off, replacement order, cancelled/failed status, or a guest order while gameballTrackGuestOrders is off or gameballGuestOrderMode is SKIP. FAILED - the attempt errored transiently and the Gameball Order Retry job will try again. FAILED_PERMANENT - the attempt failed for a reason that will fail identically every time (malformed payload, invalid transaction time); never retried. RETRY_EXHAUSTED - the retry job used up gameballRetryMaxAttempts without success; never retried. Refund state is tracked separately on gbRefundState.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbGameballOrderId">
                <display-name xml:lang="x-default">Gameball Order Id</display-name>
                <description xml:lang="x-default">The exact orderId value sent to Gameball's integrations/orders endpoint. Only set when gbTrackState is TRACKED. Needed so a future refund feature can replay this same id.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbCustomerId">
                <display-name xml:lang="x-default">Gameball Customer Id</display-name>
                <description xml:lang="x-default">The exact customerId value sent to Gameball for this order. Only set when gbTrackState is TRACKED. Kept for auditability.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbLastError">
                <display-name xml:lang="x-default">Gameball Last Error</display-name>
                <description xml:lang="x-default">The error/result message from the most recent failed Gameball sync attempt on this order. Only set when gbTrackState is FAILED. Surfaced here for support visibility since there is no Gameball dashboard in this iteration.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <!-- item 05 -->
            <attribute-definition attribute-id="gbCustomerIdSource">
                <display-name xml:lang="x-default">Gameball Customer Id Source</display-name>
                <description xml:lang="x-default">Which rung of the identity ladder produced the gbCustomerId sent for this order: PROFILE (the order's own registered customer profile), LOGIN_MATCH (a guest order whose email resolved to an existing customer login), GUEST_PER_ORDER (a derived id from the order number) or GUEST_PER_EMAIL (a derived id from the hashed customer email). Only set when gbTrackState is TRACKED. Written for support: a GUEST_PER_ORDER value while gameballGuestOrderMode is PER_EMAIL means the order had no usable email and the empty-identifier fallback fired.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <!-- item 06 -->
            <attribute-definition attribute-id="gbLastErrorCode">
                <display-name xml:lang="x-default">Gameball Last Error Code</display-name>
                <description xml:lang="x-default">The Gameball error code from the most recent failed ORDER-TRACKING attempt (e.g. 3000, 9004), or a synthetic transport token when Gameball returned no envelope: TIMEOUT, HTTP_500, SVC_UNAVAILABLE, PROBE_FAILED, PAYLOAD_BUILD_FAILED, EXCEPTION, NO_RESULT. This is what the Gameball Order Retry job classifies on - a 3000 will fail identically forever and is settled permanently without another call, a TIMEOUT is verified and re-sent. Refund attempts are tracked separately (see gbRefundState).</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbLastRequestId">
                <display-name xml:lang="x-default">Gameball Last Request Id</display-name>
                <description xml:lang="x-default">The requestId from the most recent Gameball error envelope for an ORDER-TRACKING attempt. This is the token Gameball support asks for; it is the whole reason the field exists.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRetryAttempts">
                <display-name xml:lang="x-default">Gameball Track Attempts</display-name>
                <description xml:lang="x-default">How many Gameball ORDER-TRACKING attempts this order has consumed, including the original confirmation-page attempt. When it reaches gameballRetryMaxAttempts the order is settled to RETRY_EXHAUSTED and is never re-sent. Not incremented for failures the merchant caused (bad credentials, Gameball disabled) or that SFCC's own rate limiter/circuit breaker caused. This counts order-tracking attempts only - it is unrelated to any refund attempt ledger.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>0</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gbLastAttemptAt">
                <display-name xml:lang="x-default">Gameball Last Attempt At</display-name>
                <description xml:lang="x-default">Timestamp of the most recent Gameball ORDER-TRACKING attempt, successful or not. Operator visibility only - never used as a search predicate. Distinct from GameballErasureRequest's own gbLastAttemptAt field, which tracks a customer erasure mandate, not an order.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbNextRetryAt">
                <display-name xml:lang="x-default">Gameball Next Retry Not Before</display-name>
                <description xml:lang="x-default">Earliest time the Gameball Order Retry job may attempt this order again. Applied as an exponential backoff so a Gameball outage does not burn the whole attempt budget in consecutive job runs. Empty means "due now". Never used as a search predicate - it is evaluated in the job callback, on orders the state predicate already matched.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <!-- item 07 -->
            <attribute-definition attribute-id="gbTrackedAt">
                <display-name xml:lang="x-default">Gameball Tracked At</display-name>
                <description xml:lang="x-default">The moment this order was successfully sent to Gameball. Only set when gbTrackState is TRACKED, and never changed afterwards. This is the anchor the Gameball Refund Detector job scans on, so an order without it is invisible to refund detection forever.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbTrackedTotalPaid">
                <display-name xml:lang="x-default">Gameball Tracked Total Paid</display-name>
                <description xml:lang="x-default">The exact totalPaid figure reported to Gameball for this order. Only set when gbTrackState is TRACKED. This is the ceiling for cumulative refunds: gbRefundedAmount may never exceed it. Stored rather than recomputed because the order can be edited after tracking and a recomputed figure would silently move the ceiling.</description>
                <type>double</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbTrackedCurrency">
                <display-name xml:lang="x-default">Gameball Tracked Currency</display-name>
                <description xml:lang="x-default">The ISO currency code the order was in when it was reported to Gameball. Only set when gbTrackState is TRACKED. Gameball has no currency field on any endpoint - currency is an account-level setting - so this exists purely so a refund can refuse to send when the order currency no longer matches what was tracked, rather than reversing the wrong number of units.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundState">
                <display-name xml:lang="x-default">Gameball Refund State</display-name>
                <description xml:lang="x-default">Cumulative refund position for this order: NONE (nothing reversed), PARTIAL (some amount reversed, more possible), FULL (fully reversed - no further refund will ever be sent), MANUAL_REVIEW (a guard refused to guess and no call was made - a human must decide), FAILED (delivery was attempted and permanently rejected by Gameball). Set to NONE at track time. Only the Gameball refund modules write this.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundedAmount">
                <display-name xml:lang="x-default">Gameball Refunded Amount</display-name>
                <description xml:lang="x-default">Running total of refund amounts already accepted by Gameball for this order, in the order currency. A full reversal sets this to gbTrackedTotalPaid. Compared against gbTrackedTotalPaid before every partial refund; on breach the refund is routed to MANUAL_REVIEW and never sent, never clamped.</description>
                <type>double</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>0</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundSeq">
                <display-name xml:lang="x-default">Gameball Refund Sequence</display-name>
                <description xml:lang="x-default">Monotonic counter used to mint a unique refundTransactionId for each refund on this order. Incremented at the moment a refund is RECORDED, inside the same transaction that appends it to gbRefundLedger - never at delivery time. Allocating it at delivery would let two refunds recorded before either was sent compute the same id, at which point Gameball answers 9004, the second refund looks like a duplicate of the first, and a real refund is silently never clawed back.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
                <default-value>0</default-value>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundLedger">
                <display-name xml:lang="x-default">Gameball Refund Ledger</display-name>
                <description xml:lang="x-default">JSON array of every refund recorded against this order, one object per refund, each carrying its own refundTransactionId, transactionTime, amount, state and last Gameball response. This is the idempotency record and the retry queue for this order and nothing else - it is deliberately not a general-purpose event queue. Never edit it by hand: a value that does not parse as JSON puts the order into MANUAL_REVIEW and stops all further refunds on it.</description>
                <type>text</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundPendingAt">
                <display-name xml:lang="x-default">Gameball Refund Pending Since</display-name>
                <description xml:lang="x-default">Set when this order has at least one refund that has been recorded but not yet accepted by Gameball; cleared (set to null) once every recorded refund has reached a terminal state. The Gameball Refund Detector job queries on this to find work left over from an earlier run, so an order with a stuck refund and no value here would never be retried.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRefundLastError">
                <display-name xml:lang="x-default">Gameball Refund Last Error</display-name>
                <description xml:lang="x-default">The Gameball error code, message and requestId from the most recent failed refund delivery on this order. Deliberately separate from gbLastError, which belongs to order tracking - overloading one field would make an order-tracking failure and a refund failure indistinguishable in Business Manager. Always quote the requestId when contacting Gameball support.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <!-- item 08 -->
            <attribute-definition attribute-id="gbHoldReference">
                <display-name xml:lang="x-default">Gameball Hold Reference</display-name>
                <description xml:lang="x-default">The Gameball hold reference redeemed on this order, if any. Normally copied automatically from the order's own PriceAdjustment (see PriceAdjustment.custom.gbHoldReference); written directly here only as a fallback if that automatic copy did not happen. This is what orderPayload.js reads to populate redemption.pointsHoldReference on retry.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbRedeemedPoints">
                <display-name xml:lang="x-default">Gameball Redeemed Points</display-name>
                <description xml:lang="x-default">Points redeemed on this order via the hold/PriceAdjustment path. Distinct from any points earned on this order. BM-visible summary - the authoritative per-adjustment record is PriceAdjustment.custom.gbPointsRedeemed.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
        </custom-attribute-definitions>
        <group-definitions>
            <attribute-group group-id="GameballOrder">
                <display-name xml:lang="x-default">Gameball</display-name>
                <attribute attribute-id="gbTrackState"/>
                <attribute attribute-id="gbGameballOrderId"/>
                <attribute attribute-id="gbCustomerId"/>
                <attribute attribute-id="gbLastError"/>
                <attribute attribute-id="gbCustomerIdSource"/>
                <!-- appended by item 06 -->
                <attribute attribute-id="gbLastErrorCode"/>
                <attribute attribute-id="gbLastRequestId"/>
                <attribute attribute-id="gbRetryAttempts"/>
                <attribute attribute-id="gbLastAttemptAt"/>
                <attribute attribute-id="gbNextRetryAt"/>
                <!-- appended by item 07 -->
                <attribute attribute-id="gbTrackedAt"/>
                <attribute attribute-id="gbTrackedTotalPaid"/>
                <attribute attribute-id="gbTrackedCurrency"/>
                <!-- appended by item 08 -->
                <attribute attribute-id="gbHoldReference"/>
                <attribute attribute-id="gbRedeemedPoints"/>
            </attribute-group>
            <!-- item 07 -->
            <attribute-group group-id="GameballOrderRefund">
                <display-name xml:lang="x-default">Gameball Refunds</display-name>
                <attribute attribute-id="gbRefundState"/>
                <attribute attribute-id="gbRefundedAmount"/>
                <attribute attribute-id="gbRefundSeq"/>
                <attribute attribute-id="gbRefundLedger"/>
                <attribute attribute-id="gbRefundPendingAt"/>
                <attribute attribute-id="gbRefundLastError"/>
            </attribute-group>
        </group-definitions>
    </type-extension>
    <type-extension type-id="Profile">
        <custom-attribute-definitions>
            <!-- item 03 -->
            <attribute-definition attribute-id="gbSyncState">
                <display-name xml:lang="x-default">Gameball Sync State</display-name>
                <description xml:lang="x-default">Outcome of the most recent Gameball customer upsert attempt for this profile. SYNCED - Gameball accepted the payload. FAILED - the call errored, was rejected, or was rate-limited; it is retried by the Gameball Customer Delta job for as long as the profile stays inside that job's lookback window, unless the rejection was permanent. SKIPPED - a gating rule stopped the call before it was made: this entry point is switched off, the profile has no customer number, or it has no email while Require an Email Before Syncing a Customer is on. A profile that already reads SYNCED is never relabelled SKIPPED just because an entry point is switched off, because that says nothing about whether the customer reached Gameball.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbSyncHash">
                <display-name xml:lang="x-default">Gameball Sync Payload Hash</display-name>
                <description xml:lang="x-default">Versioned hash of the exact payload body Gameball last ACCEPTED for this profile, or last permanently rejected. Written only after a successful or permanently-rejected call, never after a retryable failure. A sync path that recomputes this hash and finds it unchanged makes no API call at all - this is the only thing stopping the hourly delta job from re-POSTing every unchanged profile in its window. Clearing this value forces a resync on the next sync of this profile, and is the supported way to push a stuck profile through again.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbLastSyncAt">
                <display-name xml:lang="x-default">Gameball Last Sync Attempt</display-name>
                <description xml:lang="x-default">When a Gameball customer upsert was last ATTEMPTED for this profile, successful or not. Read gbSyncState to know the outcome. Deliberately not written when a profile was found unchanged and no call was made: writing it there would bump the profile's last-modified date on every sweep and make the sweep permanently feed itself. For the same reason it is not refreshed while an identical failure repeats from the same entry point, so on a profile stuck in FAILED it reads as when that failure was first recorded - refreshing it every attempt would hold the profile inside the delta job's lookback window forever and it would never stop being retried.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbGameballId">
                <display-name xml:lang="x-default">Gameball Id</display-name>
                <description xml:lang="x-default">The gameballId returned by Gameball's integrations/customers 200 response. A change in this value is the only local signal that Gameball merged this profile into another one through channel merging.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbSyncSource">
                <display-name xml:lang="x-default">Gameball Sync Source</display-name>
                <description xml:lang="x-default">Which entry point performed the most recent sync attempt: ACCOUNT_SUBMIT_REGISTRATION, ACCOUNT_SAVE_PROFILE, ORDER_CREATE_ACCOUNT, OCAPI_SHOP_POST, OCAPI_SHOP_PATCH, OCAPI_DATA_POST, OCAPI_DATA_PUT, OCAPI_DATA_PATCH, DELTA_JOB, or MERCHANT_API (merchant code calling the Gameball customer API directly). There is no Business Manager dashboard in this iteration, so this attribute is how support answers "how did this customer get to Gameball".</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbLastSyncError">
                <display-name xml:lang="x-default">Gameball Last Sync Error</display-name>
                <description xml:lang="x-default">Compact summary of the most recent failed Gameball customer upsert, as httpStatus|gameballCode|requestId|message. Only set when gbSyncState is FAILED, and cleared on the next successful sync. requestId is the token Gameball support asks for, which is why it is a field of its own rather than buried in the message. The payload is never stored here.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
        </custom-attribute-definitions>
        <group-definitions>
            <attribute-group group-id="GameballCustomer">
                <display-name xml:lang="x-default">Gameball</display-name>
                <attribute attribute-id="gbSyncState"/>
                <attribute attribute-id="gbSyncHash"/>
                <attribute attribute-id="gbLastSyncAt"/>
                <attribute attribute-id="gbGameballId"/>
                <attribute attribute-id="gbSyncSource"/>
                <attribute attribute-id="gbLastSyncError"/>
            </attribute-group>
        </group-definitions>
    </type-extension>
    <!-- item 08: Pay with Points. Not added to the existing Order/Profile
         type-extensions above because Basket and PriceAdjustment have never
         been extended by this cartridge before now. -->
    <type-extension type-id="Basket">
        <custom-attribute-definitions>
            <attribute-definition attribute-id="gbHoldReference">
                <display-name xml:lang="x-default">Gameball Hold Reference</display-name>
                <description xml:lang="x-default">The active Gameball transactions/hold holdReference for this basket, if any. Set the moment a hold is created, cleared the moment it is released or converted into an order at placement. Empty means no live hold - this is the field every other module in this feature checks first.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbHoldAmount">
                <display-name xml:lang="x-default">Gameball Hold Amount</display-name>
                <description xml:lang="x-default">The currency amount of the active hold, in the basket's currency - exactly the holdAmount Gameball returned, never recomputed. This is the source of truth the PriceAdjustment's own price is set from and reconciled against on every basket change.</description>
                <type>double</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbHoldPointsRedeemed">
                <display-name xml:lang="x-default">Gameball Hold Points Redeemed</display-name>
                <description xml:lang="x-default">The points count of the active hold - exactly the holdEquivalentPoints Gameball returned. Displayed to the shopper and copied onto the Order's own gbRedeemedPoints at placement without a second lookup.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbHoldExpiresAt">
                <display-name xml:lang="x-default">Gameball Hold Expires At (Estimated)</display-name>
                <description xml:lang="x-default">NOT AUTHORITATIVE. Neither the hold-create nor hold-status Gameball endpoint documents an expiry timestamp in its response, so this is computed as "now + gameballRedemptionAssumedHoldTtlMinutes" purely for a shopper-facing countdown. Never read by any release or reconciliation logic - Gameball's own dashboard timeout is the only real enforcement.</description>
                <type>datetime</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
        </custom-attribute-definitions>
        <group-definitions>
            <attribute-group group-id="Gameball">
                <display-name xml:lang="x-default">Gameball</display-name>
                <attribute attribute-id="gbHoldReference"/>
                <attribute attribute-id="gbHoldAmount"/>
                <attribute attribute-id="gbHoldPointsRedeemed"/>
                <attribute attribute-id="gbHoldExpiresAt"/>
            </attribute-group>
        </group-definitions>
    </type-extension>
    <type-extension type-id="PriceAdjustment">
        <custom-attribute-definitions>
            <attribute-definition attribute-id="gbHoldReference">
                <display-name xml:lang="x-default">Gameball Hold Reference</display-name>
                <description xml:lang="x-default">The Gameball hold reference this price adjustment represents. Copied verbatim from the basket's gbHoldReference at creation time. SFCC copies Basket price adjustments, including their custom attributes, onto the Order automatically at order creation - this is how orderPayload.js finds the redemption block to report at order-tracking time.</description>
                <type>string</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
            <attribute-definition attribute-id="gbPointsRedeemed">
                <display-name xml:lang="x-default">Gameball Points Redeemed</display-name>
                <description xml:lang="x-default">The points count this discount represents, copied from the basket's gbHoldPointsRedeemed at creation time. Informational - the actual burn happens via the redemption.pointsHoldReference reported at order-tracking time, not via this field.</description>
                <type>int</type>
                <mandatory-flag>false</mandatory-flag>
                <externally-managed-flag>false</externally-managed-flag>
            </attribute-definition>
        </custom-attribute-definitions>
        <group-definitions>
            <attribute-group group-id="Gameball">
                <display-name xml:lang="x-default">Gameball</display-name>
                <attribute attribute-id="gbHoldReference"/>
                <attribute attribute-id="gbPointsRedeemed"/>
            </attribute-group>
        </group-definitions>
    </type-extension>
</metadata>
